var app = app || {};

$(function () {
	'use strict';

	// kick things off by creating the `App` ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

	var calApp = new app.Calendar(); // self-rendering in initialize()
});
