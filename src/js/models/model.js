/*global Backbone */
var app = app || {};

(function () {
	'use strict';

	// Model
	// ----------

	app.model = Backbone.Model.extend({
		// Default attributes for the model
		defaults: {}
	});
})();
