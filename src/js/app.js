/*global $ */
/*jshint unused:false */
var app = app || {};

$(function () {
	'use strict';

	// kick things off by creating the `App`
	var calApp = new app.appView(); // self-rendering in initialize()
});
